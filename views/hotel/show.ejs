<h2><%= hotel.name %></h2>

<img height="150" width="150" src="<%= hotel.photoPath %>">

<ul>
    <li>Address: <%= hotel.address %></li>
    <li>Daily Price: € <%= hotel.dailyPrice %></li>
    <li>Number of Rooms: <%= hotel.numberOfRooms %></li>
</ul>

<% if (hotel.rooms.length > 0) { %>
    <h2>Rooms:</h2>
    <% hotel.rooms.forEach(room => { %>
        <div> <!-- fix this!! -->
            <h4>Room Number: <%= room.roomNumber %></h3>
            <h4>Is Vacant: <%= room.isVacant %></h3>
        </div>
    <% }) %>
<% } %>

<a href="/hotel/<%= hotel.id %>/edit">Edit</a>
<%- include('../partials/deleteForm.ejs', { url: `/hotel/${hotel.id}` }) %>
