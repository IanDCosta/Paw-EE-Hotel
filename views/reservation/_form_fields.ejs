<!-- partial file! 
    theorically you cant create reservations in backend!
-->

<div>
<label>Code</label>
<input type="text" name="code" value="<%= reservation.code %>">
</div>

<div>
    <label>Location*</label>
    <select name="location">
        <% hotels.forEach(hotel => { %>
            <% if(hotel.rooms.some((obj) => obj.isVacant === true)){ %>
                <option label="<%= hotel.name %>" value="<%= hotel.id %>"></option>
            <% } %>
        <% }) %>
    </select>
</div>

<div>
<label>Begin Date:</label>
<input type="date" name="dateBegin" 
    value="<%= reservation.dateBegin == null ? '' : reservation.dateBegin.toISOString().split('T')[0] %>">
</div>

<div>
<label>End Date</label>
<input type="date" name="dateEnd" 
value="<%= reservation.dateEnd == null ? '' : reservation.dateEnd.toISOString().split('T')[0] %>">
</div>

<div>
    <label>Pet*</label>
    <select name="pet">
        <% pets.forEach(pet => { %>
            <option label="<%= pet.name %>" value="<%= pet.id %>"></option>
        <% }) %>
    </select>
</div>